# Points at the persistent realtime-server (see /realtime-server), which
# handles Discord auth, sessions, and live ATIS — NOT the legacy Vercel
# serverless backend, which cannot hold the 24data WebSocket open.
VITE_API_BASE_URL=http://localhost:8080
